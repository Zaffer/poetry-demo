From file explorer open Integrated Terminal.

`poetry -V`

`poetry new my-folder --name my-package`

`cd my-folder`

`code .`


Opens Visual Studio Code.

Delete pyproject.toml file.

`poetry init`

`poetry shell`

`poetry install`
